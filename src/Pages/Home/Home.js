import React from 'react';
import MainCarousel from '../../Utilities/MainCarousel';

const Home = () => {
    return (
        <div>
            <MainCarousel />
        </div>
    );
};

export default Home;