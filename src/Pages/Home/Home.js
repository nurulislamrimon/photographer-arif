import React from 'react';
import CustomCarousel from './CustomCarousel/CustomCarousel';

const Home = () => {
    return (
        <div>
            <CustomCarousel />
        </div>
    );
};

export default Home;